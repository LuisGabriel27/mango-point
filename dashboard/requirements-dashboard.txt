"""
MangoPoint Dashboard — Python (Dash + Plotly)
==============================================
Requirements for the Python dashboard.
Install with:  pip install -r requirements-dashboard.txt
"""
# Core dashboard framework
dash>=2.14.0
dash-bootstrap-components>=1.5.0
dash-leaflet>=1.0.15

# HTTP client to talk to FastAPI backend
requests>=2.31.0

# Data handling (already in main requirements.txt)
pandas>=2.0
numpy>=1.24

# Plotting
plotly>=5.18.0

# GIS raster overlay
rasterio>=1.3
Pillow>=10.0
